name: e-commerce

services:
  postgres:
    image: postgres:18.6-alpine3.24
    environment:
      POSTGRES_DB: e_commerce_dev
      POSTGRES_USER: e_commerce
      POSTGRES_PASSWORD: e_commerce_local_only
    ports:
      - '127.0.0.1:5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql
      - ./infrastructure/postgres/init/01-create-test-database.sql:/docker-entrypoint-initdb.d/01-create-test-database.sql:ro
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready --username e_commerce --dbname e_commerce_dev']
      interval: 2s
      timeout: 5s
      retries: 15
      start_period: 5s
    shm_size: 128mb

volumes:
  postgres-data:
